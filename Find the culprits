Find the culprit:
alert("I'm JavaScript!"); //this line contains no error it display first .

Find the culprit and invoke the alert
alert('Hello'); // this line is not having semicolon

Explain the below how it works
alert(`Wor
 ld`)//this line using template literals it returns the order that we give and also not having semicolon.
alert(3 +
1
+ 2); //ans:6 // this is multiple line code and its working return the addition of value.

Fix the below to alert Guvi geek
let admin=9, fname=10.5;
fname = "Guvi";
lname = "geek"
admin = fname+" "+lname; 
alert(admin); // "Guvi geek"

Fix the below to alert hello Guvi geek
let fname=10.5;
fname = "Guvi";
lname = "geek"
let name = fname+" "+lname; 
alert(`hello ${name}`); //hello Guvi geek

Fix the below to alert sum of two numbers
let a = parseInt(prompt("First number?"));
let b = parseInt(prompt("Second number?"));
alert(a + b); //5+5 ans:10

If you run the below scritpt you will get “Code is Blasted”

Explain Why the Code is blasted and 
how to diffuse it and get “Diffused”.
var a = "2" > "12"; //because at first it compares the string 2 with 1.
//Don't touch below this
if (a) {
  console.log("Code is Blasted")
}
else
{
  console.log("Diffused")
}

How to get the success in console.
let a =parseInt(prompt("Enter a number?","0")); //parseInt() will take a string, either as a constant 
//inside quotation marks or a variable expression will turn it into a whole number. 
//Don't modify any code below this
if (a) {
 console.log( 'OMG it works for any number inc 0' );
}
else
{
 console.log( "Success" );
}

How to get the correct score in console.
let value = parseInt(prompt('How many runs you scored in this ball'));
if (value === 4) {
      console.log("You hit a Four");
} else if (value === 6) {
      console.log("You hit a Six");
} else {
      console.log("I couldn't figure out");
}

Fix the code to welcome the Employee
let login = 'Employee';
let message = (login == 'Employee') ? 'welcome':
  (login == 'Director') ? 'Greetings' :
  (login == '') ? 'No login' :
  '';
console.log(message);

Fix the code to welcome the boss
You cant change the value of the msg
let message;
if (null || 2 || undefined )
{
  message = "welcome boss";
}
else
{
  message = "Go away";
}
  console.log(message);

Fix the code to welcome the boss
let message;
let lock=null;
//Dont change any code below this
if (null || lock || undefined )
{
  message = "Go away";
}
else
{
 message = "welcome";
}
  console.log(message);

Fix the code to welcome the boss
let message;
let lock=undefined;
//Dont change any code below this
if (lock && " " || undefined )
{
  message = "Go away";
}
else
{
 message = "welcome";
}
console.log(message);

Change the code to print
3
2
1
You can change only 2 characters
let i = 3;
while (i) {
  console.log(i--);
}

Change the code to print 1 to 10 in 4 lines
for(let i=1; i<=10; i++){
    console.log(i);
}

Change the code to print even numbers
You are allowed to modify only one character
for (let num = 2; num <= 20; num += 2) {
    console.log(num)
  }

Change the code to print all the gifts
let gifts = ["teddy bear", "drone", "doll"];
for (let i = 0; i < 3; i++) {
  console.log(`Wrapped ${gifts[i]} and added a bow!`);
}

Fix the code to disarm the bomb
let countdown = 100;
while (countdown > 0) {
  countdown--;
  if(countdown == 0)
  {
   console.log("disarm the bomb");
  }
}

Whats the msg printed and why?
var lemein = "0";
var lemeout = 0;
var msg = "";
if (lemein) {
 msg += "hi";//because string 0("0") is cnsidered true.
 }
if (lemeout) {
 msg += 'Hello';
}
console.log(msg);

Whats the msg printed and why? Guess you answer before running it.
because string 0("0") is cnsidered 
ans: hi
var lemein = "0";
var lemeout = 0;
var msg = "";
if (lemein) {
 msg += "hi";
 }
if (lemeout) {
 msg += 'Hello';
}
console.log(msg);
